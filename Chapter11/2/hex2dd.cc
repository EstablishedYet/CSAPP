#include<unistd.h>
#include<cstdio>
#include<cstdlib>
#include<arpa/inet.h>
#include<cstring>
#include<cmath>

int main(int argc,char **argv)
{
    // char *arg=(char *)argv[1]+2;
    // // printf("%d",strlen(arg));
    // size_t length=strlen(arg);
    // uint16_t accum=0;
    // for(int i=0;i<length;++i)
    // {
    //     accum+=(arg[i]-'0')*pow(16,length-1-i);
    // }
    // printf("%d\n",accum);
    // // char piv;
    // // for(int i=0;i<((int)(length/2)-1);++i)
    // // {
    // //     piv=arg[i];
    // //     arg[i]=arg[length-1-i];
    // //     arg[length-1-i]=piv;
    // // }

    // return 0;
    
}